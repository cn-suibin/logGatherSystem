
- type: log

  paths:
    - /var/log/boot.log
  fields:
       logtype: syslog
#  scan_frequency: 10s
  # 日志多行合并采集
    #multiline.pattern: '^\['
    #multiline.negate: true
#    multiline.match: after
