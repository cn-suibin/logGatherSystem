- type: log

  paths:
    - /app/logs/platform_error.log.2020-10-19.log
    - /etc/nginx/logs/error.log
  fields:
       logtype: syslog
#  scan_frequency: 10s
  # 日志多行合并采集
    #multiline.pattern: '^\['
    #multiline.negate: true
#    multiline.match: after
